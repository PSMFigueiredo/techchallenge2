version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - DB_CONNECTION=mongodb+srv://priscylastefanea:pristefanea@blogger.vca33ds.mongodb.net/?retryWrites=true&w=majority&appName=Blogger
      - PORT=3000
